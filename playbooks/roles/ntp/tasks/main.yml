---
- name: Install chrony
  yum:
    name: chrony
    state: present

- name: Copy chrony.conf
  template:
    src: chrony.conf.j2
    dest: /etc/chrony.conf

- name: Start and enable chronyd
  service:
    name: chronyd
    state: started
    enabled: yes

